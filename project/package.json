{
  "name": "sample-node-app",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node bin/www",
    "prestart": "npm install",
    "pretest": "npm install",
    "test": "cross-env PORT=8001 mocha --reporter mocha-multi-reporters --reporter-options configFile=config.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^9.2.0",
    "mocha-junit-reporter": "^2.2.1",
    "mocha-multi-reporters": "^1.5.1"
  },
  "dependencies": {
    "chai-http": "^4.4.0",
    "cross-env": "^7.0.3",
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  }
}
